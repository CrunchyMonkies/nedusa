{
  "name": "@nedusa/server",
  "version": "2.21.1",
  "private": true,
  "type": "module",
  "description": "nedusa HTTP application (NestJS)",
  "main": "dist/main.js",
  "scripts": {
    "build": "rimraf dist && tsc --build",
    "watch": "tsc --build --watch",
    "start": "node dist/main.js",
    "dev": "tsc --build && node dist/main.js"
  },
  "dependencies": {
    "@nestjs/common": "^12.0.4",
    "@nestjs/core": "^12.0.4",
    "@nestjs/platform-express": "^12.0.4",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.0"
  }
}
